# 前端 UI 重构指南

> 目的：把现有前端的视觉风格迁移到 Claude.ai 风格的 Anthropic 设计系统（暖色调、Anthropic Sans、克制的色彩语义）。
> 
> 用法：把整份文档喂给 Claude Code，让它按文档操作。

---

## 第一步：建立设计 token（一次性，最重要）

所有颜色、字号、圆角都从 token 取，不要在组件里写死十六进制颜色。

### 1.1 替换 `frontend/src/index.css`

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

/* ===== Design tokens ===== */
:root {
  /* 背景 - 注意 secondary 是温暖的米白色，这是整个"棕色高级感"的来源 */
  --color-bg-primary: #FFFFFF;       /* 卡片、对话框 */
  --color-bg-secondary: #F5F4EE;     /* 页面整体背景（暖米白）*/
  --color-bg-tertiary: #F0EEE6;      /* 更深一档的米色，用于强调表面 */

  /* 文本 - 注意不是纯黑，是带暖色的深棕 */
  --color-text-primary: #1F1E1D;     /* 主文本 */
  --color-text-secondary: #6B6864;   /* 次要文本、标签 */
  --color-text-tertiary: #8B8881;    /* 占位符、辅助提示 */

  /* 边框 - 极低对比度的暖灰 */
  --color-border-tertiary: rgba(31, 30, 29, 0.08);  /* 默认 */
  --color-border-secondary: rgba(31, 30, 29, 0.16); /* hover/强调 */
  --color-border-primary: rgba(31, 30, 29, 0.24);

  /* 语义色（统一使用，不要自己临时调） */
  --color-success-bg: #EAF3DE;
  --color-success-text: #3B6D11;
  --color-success-border: #639922;

  --color-warning-bg: #FAEEDA;
  --color-warning-text: #854F0B;
  --color-warning-border: #BA7517;

  --color-danger-bg: #FCEBEB;
  --color-danger-text: #A32D2D;
  --color-danger-border: #E24B4A;

  --color-info-bg: #E6F1FB;
  --color-info-text: #185FA5;
  --color-info-border: #378ADD;

  /* Claude 标志性的暖珊瑚色 - 用作主操作按钮的强调色 */
  --color-accent: #C15F3C;
  --color-accent-hover: #A84F30;

  /* 字体 */
  --font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC",
               "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
  --font-mono: ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas, monospace;

  /* 圆角 */
  --radius-sm: 6px;
  --radius-md: 8px;
  --radius-lg: 12px;
}

@media (prefers-color-scheme: dark) {
  :root {
    --color-bg-primary: #262624;
    --color-bg-secondary: #1F1E1D;
    --color-bg-tertiary: #1A1918;

    --color-text-primary: #F5F4EE;
    --color-text-secondary: #B4B2A9;
    --color-text-tertiary: #888780;

    --color-border-tertiary: rgba(245, 244, 238, 0.1);
    --color-border-secondary: rgba(245, 244, 238, 0.18);
    --color-border-primary: rgba(245, 244, 238, 0.28);

    --color-success-bg: #173404;
    --color-success-text: #C0DD97;
    --color-warning-bg: #412402;
    --color-warning-text: #FAC775;
    --color-danger-bg: #501313;
    --color-danger-text: #F7C1C1;
    --color-info-bg: #042C53;
    --color-info-text: #B5D4F4;
  }
}

/* ===== 基础样式 ===== */
html, body {
  margin: 0;
  background: var(--color-bg-secondary);
  color: var(--color-text-primary);
  font-family: var(--font-sans);
  font-size: 14px;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}

* { box-sizing: border-box; }

/* 字号规范 - 整个应用只用这几档 */
h1 { font-size: 22px; font-weight: 500; }
h2 { font-size: 18px; font-weight: 500; }
h3 { font-size: 16px; font-weight: 500; }
/* 正文 14px，小字 12px，最小 11px。永远不用 >500 的字重 */
```

### 1.2 替换 `frontend/tailwind.config.js`

```js
/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{ts,tsx}"],
  theme: {
    extend: {
      colors: {
        bg: {
          primary: "var(--color-bg-primary)",
          secondary: "var(--color-bg-secondary)",
          tertiary: "var(--color-bg-tertiary)",
        },
        text: {
          primary: "var(--color-text-primary)",
          secondary: "var(--color-text-secondary)",
          tertiary: "var(--color-text-tertiary)",
        },
        border: {
          DEFAULT: "var(--color-border-tertiary)",
          secondary: "var(--color-border-secondary)",
          primary: "var(--color-border-primary)",
        },
        success: {
          bg: "var(--color-success-bg)",
          text: "var(--color-success-text)",
          border: "var(--color-success-border)",
        },
        warning: {
          bg: "var(--color-warning-bg)",
          text: "var(--color-warning-text)",
          border: "var(--color-warning-border)",
        },
        danger: {
          bg: "var(--color-danger-bg)",
          text: "var(--color-danger-text)",
          border: "var(--color-danger-border)",
        },
        info: {
          bg: "var(--color-info-bg)",
          text: "var(--color-info-text)",
          border: "var(--color-info-border)",
        },
        accent: {
          DEFAULT: "var(--color-accent)",
          hover: "var(--color-accent-hover)",
        },
      },
      borderRadius: {
        sm: "var(--radius-sm)",
        md: "var(--radius-md)",
        lg: "var(--radius-lg)",
      },
      fontFamily: {
        sans: "var(--font-sans)",
        mono: "var(--font-mono)",
      },
    },
  },
  plugins: [],
};
```

---

## 第二步：核心视觉规范

Claude Code 在写所有组件时必须遵守：

### 2.1 形态规范

- **边框统一 0.5px**：`border: 0.5px solid var(--color-border-tertiary)`，不是 1px
- **卡片圆角 12px**（`rounded-lg`），按钮和输入框 8px（`rounded-md`）
- **不要用阴影**（除了 input focus ring）：`box-shadow: none`
- **不要用渐变**：所有背景都是平涂
- **不要超过两个字重**：400 regular + 500 medium。永远不用 600/700/bold

### 2.2 排版规范

- **句首大写**（中文不适用，但英文标签如 "Cases" 不是 "CASES"）
- **不在句中加粗**。粗体只用于标题和数据标签
- 数字要用 `font-variant-numeric: tabular-nums`，避免抖动

### 2.3 颜色语义（强制）

整个应用中三个分类的颜色锁死：

|类别|背景|文字|Tailwind 写法|
|---|---|---|---|
|子宫正常大|`bg-success-bg`|`text-success-text`|`bg-success-bg text-success-text`|
|子宫内膜癌|`bg-danger-bg`|`text-danger-text`|`bg-danger-bg text-danger-text`|
|息肉|`bg-warning-bg`|`text-warning-text`|`bg-warning-bg text-warning-text`|

**这三个颜色不许用于任何其他用途**（不能用红色做"删除"按钮、不能用绿色做"成功提示"——因为它们已经被分类语义占用了）。

### 2.4 间距规范

- 页面级容器：左右 `padding: 16px`
- 卡片内部：`padding: 14px 16px` 或 `1rem 1.25rem`
- 元素之间纵向间距：`gap: 12px`（紧凑）或 `gap: 16px`（标准）
- 表格行高：`padding: 9px 10px`（紧凑场景）

---

## 第三步：核心组件参考代码

按这个模子写其他组件。

### 3.1 顶部导航 `components/Layout.tsx`

注意：**已去掉右上角的"王医生"标签**。

```tsx
import { NavLink, Outlet } from "react-router-dom";

const navItems = [
  { to: "/predict", label: "单例推理" },
  { to: "/batch", label: "批量推理" },
  { to: "/history", label: "历史记录" },
  { to: "/settings", label: "设置" },
];

export function Layout() {
  return (
    <div className="min-h-screen bg-bg-secondary">
      <nav className="flex items-center gap-5 px-4 py-2.5 border-b border-border bg-bg-secondary">
        <div className="flex items-center gap-2 text-[13px] font-medium">
          <span className="w-1.5 h-1.5 rounded-full bg-success-border" />
          子宫超声辅助诊断
          <span className="text-text-tertiary font-normal">v0.3</span>
        </div>
        <div className="flex gap-0.5 ml-2">
          {navItems.map((item) => (
            <NavLink
              key={item.to}
              to={item.to}
              className={({ isActive }) =>
                `px-2.5 py-1 text-xs rounded-md transition-colors ${
                  isActive
                    ? "bg-bg-primary text-text-primary font-medium"
                    : "text-text-secondary hover:text-text-primary"
                }`
              }
            >
              {item.label}
            </NavLink>
          ))}
        </div>
      </nav>
      <main className="p-4">
        <Outlet />
      </main>
    </div>
  );
}
```

### 3.2 概率条 `components/ProbabilityBars.tsx`

```tsx
type Probability = {
  label: string;
  value: number; // 0-1
  color: "danger" | "warning" | "success";
};

const colorMap = {
  danger: "var(--color-danger-text)",
  warning: "var(--color-warning-text)",
  success: "var(--color-success-text)",
};

export function ProbabilityBars({ items }: { items: Probability[] }) {
  return (
    <div className="space-y-1.5">
      {items.map((item) => (
        <div key={item.label} className="flex items-center gap-2.5 text-xs">
          <span className="w-[78px] text-text-secondary">{item.label}</span>
          <div className="flex-1 h-1.5 bg-bg-tertiary rounded-full overflow-hidden">
            <div
              className="h-full rounded-full transition-all"
              style={{
                width: `${item.value * 100}%`,
                backgroundColor: colorMap[item.color],
              }}
            />
          </div>
          <span className="w-[42px] text-right tabular-nums text-text-primary">
            {(item.value * 100).toFixed(1)}%
          </span>
        </div>
      ))}
    </div>
  );
}
```

### 3.3 推理结果卡片 `components/PredictionResult.tsx`

```tsx
import { ProbabilityBars } from "./ProbabilityBars";

type Props = {
  predictedLabel: string;          // "子宫内膜癌"
  predictedColor: "danger" | "warning" | "success";
  confidence: number;              // 0.732
  modelVersion: string;            // "ResNet18 + ERNIE-Health 融合"
  inferenceMs: number;
  probabilities: {
    cancer: number;
    polyp: number;
    normal: number;
  };
  gradcamUrl: string;
};

const colorTextMap = {
  danger: "text-danger-text",
  warning: "text-warning-text",
  success: "text-success-text",
};

export function PredictionResult({
  predictedLabel,
  predictedColor,
  confidence,
  modelVersion,
  inferenceMs,
  probabilities,
  gradcamUrl,
}: Props) {
  return (
    <div>
      <div className="text-[11px] font-medium text-text-secondary mb-1.5">
        模型判读结果
      </div>

      <div className="flex items-baseline gap-2.5 mb-3.5">
        <span className={`text-[17px] font-medium ${colorTextMap[predictedColor]}`}>
          {predictedLabel}
        </span>
        <span className="text-xs text-text-secondary">
          置信度 {(confidence * 100).toFixed(0)}% · {modelVersion} · 推理 {(inferenceMs / 1000).toFixed(1)} s
        </span>
      </div>

      <ProbabilityBars
        items={[
          { label: "子宫内膜癌", value: probabilities.cancer, color: "danger" },
          { label: "息肉", value: probabilities.polyp, color: "warning" },
          { label: "子宫正常大", value: probabilities.normal, color: "success" },
        ]}
      />

      <div className="h-px bg-border my-3.5" />

      <div className="flex justify-between text-[11px] text-text-secondary mb-1.5">
        <span>注意力热图（Grad-CAM）</span>
        <span className="font-mono">高 ← → 低</span>
      </div>
      <img
        src={gradcamUrl}
        alt="Grad-CAM heatmap"
        className="w-full aspect-[4/3] object-cover rounded-md border border-border bg-[#0e0e10]"
      />
    </div>
  );
}
```

### 3.4 类别 Badge `components/ClassBadge.tsx`

```tsx
type ClassType = "normal" | "endometrial_cancer" | "polyp";

const config = {
  normal: { label: "正常", className: "bg-success-bg text-success-text" },
  endometrial_cancer: { label: "内膜癌", className: "bg-danger-bg text-danger-text" },
  polyp: { label: "息肉", className: "bg-warning-bg text-warning-text" },
};

export function ClassBadge({
  type,
  confidence,
  showDot = false,
}: {
  type: ClassType;
  confidence?: number;
  showDot?: boolean;
}) {
  const { label, className } = config[type];
  return (
    <span
      className={`inline-flex items-center gap-1 px-1.5 py-0.5 rounded-md text-[10px] font-medium ${className}`}
    >
      {showDot && (
        <span
          className="w-1.5 h-1.5 rounded-full"
          style={{ backgroundColor: "currentColor" }}
        />
      )}
      {label}
      {confidence !== undefined && ` ${(confidence * 100).toFixed(0)}%`}
    </span>
  );
}
```

### 3.5 表格基础样式

不要用现成的 DataTable 库，手写一个克制的版本就够了：

```tsx
// 基础样式 - 在父组件直接 className 上用
<table className="w-full text-xs border-collapse [&_th]:text-left [&_th]:px-2.5 [&_th]:py-2 [&_th]:font-medium [&_th]:text-text-secondary [&_th]:text-[11px] [&_th]:border-b [&_th]:border-border [&_td]:px-2.5 [&_td]:py-2.5 [&_td]:border-b [&_td]:border-border [&_tr:last-child_td]:border-b-0">
```

### 3.6 按钮规范

```tsx
// 主操作按钮（开始分析、保存到历史）
<button className="px-4 py-2 text-xs font-medium rounded-md bg-info-text text-white hover:opacity-90 transition-opacity">
  开始分析
</button>

// 次要按钮（清空、取消）
<button className="px-3.5 py-1.5 text-xs font-medium rounded-md bg-bg-primary text-text-primary border border-border-secondary hover:bg-bg-tertiary transition-colors">
  清空
</button>
```

注意：**主按钮用蓝色（info-text）不用红色**——红色已经被"内膜癌"占用，按钮用红色会让医生潜意识紧张。

### 3.7 Metric 卡片（用于批量推理顶部统计）

```tsx
export function MetricCard({
  label,
  value,
  sub,
  color,
}: {
  label: string;
  value: string | number;
  sub?: string;
  color?: "success" | "warning" | "danger";
}) {
  const colorClass = color
    ? { success: "text-success-text", warning: "text-warning-text", danger: "text-danger-text" }[color]
    : "text-text-primary";

  return (
    <div className="bg-bg-tertiary rounded-md px-3 py-2.5">
      <div className="text-[10px] text-text-secondary mb-1 tracking-wide">{label}</div>
      <div className={`text-lg font-medium tabular-nums ${colorClass}`}>{value}</div>
      {sub && <div className="text-[10px] text-text-tertiary mt-0.5">{sub}</div>}
    </div>
  );
}
```

---

## 第四步：执行 Prompt（粘贴给 Claude Code）

```
请按下面的规范重构 frontend/ 的视觉设计，目标是 Anthropic Claude.ai 风格的暖色调极简界面。

【第一步：替换设计 token】
1. 用文档"第一步 1.1"的内容完全替换 frontend/src/index.css
2. 用文档"第一步 1.2"的内容完全替换 frontend/tailwind.config.js
3. 重启 dev server 确认 token 生效（页面背景应该变成暖米白色 #F5F4EE）

【第二步：重写顶部导航】
- 用文档"3.1 Layout.tsx"的代码替换 src/components/Layout.tsx
- 关键点：右上角不要任何医生身份标签
- 左上角是产品名 "子宫超声辅助诊断" + 版本号 + 一个绿色小圆点
- nav 项激活态是浅色背景 + 主色文字，不是下划线

【第三步：按 3.2-3.7 重写组件】
- ProbabilityBars、PredictionResult、ClassBadge、MetricCard 按文档代码实现
- 表格用文档 3.5 的内联样式

【第四步：全局检查】
重构完成后，逐项检查：
1. 全局没有任何写死的十六进制颜色（除了热图占位的 #0e0e10），全部从 CSS 变量取
2. 字重只有 400 和 500，没有 600/700
3. 边框统一 0.5px solid，不是 1px
4. 三个分类的颜色严格按规范：
   - 子宫正常大 = 绿色（success）
   - 子宫内膜癌 = 红色（danger）
   - 息肉 = 琥珀色（warning）
   - 这三个颜色不许出现在分类用途之外
5. 主操作按钮是蓝色（info-text），不是红色或绿色
6. 不要 emoji，需要图标用 lucide-react
7. 所有数字加 tabular-nums 类避免抖动

完成后告诉我，我跑一遍看效果。
```

---

## 第五步：迭代时的最佳实践

迁移完之后，**不要让 Claude Code 凭感觉调样式**。每次想改细节时，按这个流程提需求：

❌ 不好的提法：

> "把这个按钮改得好看一点"

✅ 好的提法：

> "推理页右下角的'保存到历史'按钮目前是次要按钮的样式，改成主按钮（用 info-text 作为背景，白色文字）。其他次要按钮保留不变。"

如果调整一个新颜色或新尺寸（比如想加一个新的状态色），**先在 index.css 里加 token，再让 Claude Code 用 token**——不要直接在组件里写 `style={{ color: "#xxx" }}`，否则迟早会失控。

---

## 附录：你这个方案 vs 让 Claude Code 自由发挥

如果你之前看到的前端是 Claude Code 自己生成的"现代风"（彩色渐变、大圆角、阴影、各种 emoji 图标），那是因为它没有约束就会倾向于堆视觉效果。Anthropic 的设计哲学正好相反——**克制、扁平、统一、让内容说话**。这份文档的本质是把这套约束系统化。一旦 token 和规范立起来，Claude Code 后续每次改动都会自动收敛到这个风格。